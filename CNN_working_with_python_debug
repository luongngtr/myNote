import numpy as np
import matplotlib.pyplot as plt
import matplotlib.image as mpimg

// Plot a picture with several rectangles around some object 
print('File number: %s , Number of boxes: %d' % (Train_file[ranlist[i]], B.shape[0]))
plt.figure(i)
plt.imshow(B[0,:,:,:].reshape([500,500,3]))
for j in range(B.shape[0]): 
    xplot = [A[j,0], A[j,2], A[j,2], A[j,0], A[j,0]]
    yplot = [A[j,1], A[j,1], A[j,3], A[j,3], A[j,1]]
    plt.plot(xplot,yplot)
plt.show() 
    
